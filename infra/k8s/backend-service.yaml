apiVersion: v1
kind: Service
metadata:
  name: agentops-backend
  namespace: agentops
spec:
  type: ClusterIP
  selector:
    app: agentops-backend
  ports:
    - name: http
      port: 3333
      targetPort: http
