apiVersion: v1
kind: Service
metadata:
  name: agentops-frontend
  namespace: agentops
spec:
  type: LoadBalancer
  selector:
    app: agentops-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
